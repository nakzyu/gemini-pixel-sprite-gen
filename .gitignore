# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.env
.venv/
venv/
env/

# Generated sprites (user data)
sprites/

# Per-project pipeline config (user creates via first-run setup)
sprite_spec.yaml

# Per-project roster (locked per-character designs). Stays on disk across
# sessions for persistence, but is project-specific so not part of the generic skill.
roster/

# Demo asset (regenerable, not part of skill)
demo.gif

# Local Claude Code config (per-machine)
.claude/
.claude-plugin/config.json

# OS
.DS_Store
Thumbs.db
Desktop.ini
._*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
